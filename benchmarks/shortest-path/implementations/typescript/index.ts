import{readFile,writeFile}from"node:fs/promises";const arg=(n:string)=>process.argv[process.argv.indexOf(n)+1]!;
const warmups=Number(arg("--warmup")),iterations=Number(arg("--iterations"));
type Edge={from:number;to:number;weight:number};type Query={id:number;source:number;destination:number};type Input={vertexCount:number;edges:Edge[];queries:Query[]};
const g=JSON.parse(await readFile(arg("--input"),"utf8"))as Input;
function kernel(){const adj=Array.from({length:g.vertexCount},()=>[]as Edge[]);for(const e of g.edges)adj[e.from]!.push(e);return g.queries.map(q=>{const d=Array<number>(g.vertexCount).fill(Infinity),prev=new Int32Array(g.vertexCount).fill(-1);d[q.source]=0;const h:[number,number][]=[[0,q.source]];while(h.length){const k=h[0]!,t=h.pop()!;if(h.length){h[0]=t;let i=0;for(;;){const l=2*i+1,r=2*i+2;let s=i;if(l<h.length&&h[l]![0]<h[s]![0])s=l;if(r<h.length&&h[r]![0]<h[s]![0])s=r;if(s===i)break;[h[i],h[s]]=[h[s]!,h[i]!];i=s}}const[c,u]=k;if(c!==d[u])continue;for(const e of adj[u]!){const n=c+e.weight;if(n<d[e.to]){d[e.to]=n;prev[e.to]=u;h.push([n,e.to]);let i=h.length-1;while(i>0){const p=i-1>>>1;if(h[p]![0]<=h[i]![0])break;[h[p],h[i]]=[h[i]!,h[p]!];i=p}}}}if(d[q.destination]===Infinity)return{queryId:q.id,distance:null,path:[]};const path:number[]=[];for(let x=q.destination;x>=0;x=prev[x]!)path.push(x);path.reverse();return{queryId:q.id,distance:d[q.destination],path}})}
let results;const samples=[];for(let i=-warmups;i<iterations;i++){const start=process.hrtime.bigint();results=kernel();const elapsed=process.hrtime.bigint()-start;if(i>=0)samples.push({iteration:i+1,kernelTimeNanoseconds:Number(elapsed)})}
await writeFile(arg("--output"),JSON.stringify({benchmark:"shortest-path",version:1,results}));await writeFile(arg("--timing-output"),JSON.stringify({samples}));
