Review all implementations for the selected language or runtime, establish a performance baseline using the project’s existing benchmark commands, identify bottlenecks, and apply safe optimizations without changing correctness or benchmark behavior. Use  available tools / mcp to help or repository integrations to inspect the code and validate changes. Rerun the benchmarks afterward and report the before-and-after results, including any tradeoffs or regressions.
